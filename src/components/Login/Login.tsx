import React from 'react';
import './Login.scss';

const Login: React.FC = () => {
  return (
    <div className="login">
      <button className="login__btn">Login via Google</button>
    </div>
  );
}

export default Login;
